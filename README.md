# SignalSentinel
Controller and monitor for Uniden SDS200 scanner.

Linux GUI dependencies (Debian/Ubuntu):

```bash
sudo apt-get update
sudo apt-get install -y pkg-config libgl1-mesa-dev xorg-dev
```
